<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://damonsj.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://damonsj.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-15T12:20:51+08:00</updated><id>https://damonsj.github.io/feed.xml</id><title type="html">Invisible Cities</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">python中多进程实现任务调度</title><link href="https://damonsj.github.io/programming/2026/04/06/python-task-scheduler.html" rel="alternate" type="text/html" title="python中多进程实现任务调度"/><published>2026-04-06T02:07:00+08:00</published><updated>2026-04-06T02:07:00+08:00</updated><id>https://damonsj.github.io/programming/2026/04/06/python-task-scheduler</id><content type="html" xml:base="https://damonsj.github.io/programming/2026/04/06/python-task-scheduler.html"><![CDATA[<h2 id="背景">背景</h2> <p>工作中遇到这样的场景，同一套服务同时承担在线请求与离线任务。在线服务通常面向实时用户请求，对时延和可用性要求较高；而离线服务多为批处理或异步任务，对实时性要求相对较低。当前业务中通常将同样的服务单独部署在在线集群和离线集群中，这需要同时维护两个同样的服务，并且当前系统缺乏对在线与离线服务的明确区分和统一调度机制，导致在资源紧张或负载较高的情况下，离线任务可能占用关键资源，从而影响在线服务的响应能力和稳定性。有必要设计并实现一套服务框架，对同一服务的在线与离线执行进行统一管理和调度，并支持在线服务对离线服务的抢占能力，以满足业务对实时性和稳定性的要求。这样同样的服务可以支持在线和离线任务，在线的任务优先级最高，任务会被优先调度运行，离线任务优先级低，只有在没有在线任务的时候才被调度。</p> <h2 id="设计目标">设计目标</h2> <ul> <li> <p>在线 / 离线服务统一建模 对同一业务服务，支持以”在线模式”和”离线模式”两种运行形态对外提供能力。 在调度和执行层面明确区分两类服务，而非通过约定或人工控制。</p> </li> <li> <p>在线服务抢占式执行 在线服务请求具备最高执行优先级。 当系统资源不足且存在正在运行的离线服务时，在线服务可触发抢占机制，中断或终止离线服务，确保在线请求及时执行。</p> </li> <li> <p>离线服务可控中断 当前版本离线服务中断模式且返回明确错误码。 当前版本离线任务的中断不支持回滚及重试，返回错误码后由用户决定下一步动作。</p> </li> </ul> <h2 id="整体架构">整体架构</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/scheduler/architecture-480.webp 480w,/assets/img/posts/scheduler/architecture-800.webp 800w,/assets/img/posts/scheduler/architecture-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/scheduler/architecture.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>整个系统由四层组成，从外到内分别是：<strong>HTTP 接入层</strong>、<strong>ZMQ 通信层</strong>、<strong>调度层</strong>和<strong>执行层</strong>。</p> <table> <thead> <tr> <th style="text-align: left">进程index</th> <th style="text-align: left">层级</th> <th style="text-align: left">组件</th> <th style="text-align: left">职责</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">进程1</td> <td style="text-align: left">HTTP 接入层</td> <td style="text-align: left">Gunicorn + Flask</td> <td style="text-align: left">接收 HTTP 请求，解析参数，调用 Processor</td> </tr> <tr> <td style="text-align: left">进程1</td> <td style="text-align: left">通信层</td> <td style="text-align: left">SchedulerClient (DEALER) ↔ Scheduler (ROUTER)</td> <td style="text-align: left">进程间 IPC 通信，请求/结果传递</td> </tr> <tr> <td style="text-align: left">进程2</td> <td style="text-align: left">调度层</td> <td style="text-align: left">Scheduler + ProcessManager</td> <td style="text-align: left">任务入队、优先级调度、抢占、Worker 生命周期管理</td> </tr> <tr> <td style="text-align: left">进程3</td> <td style="text-align: left">执行层</td> <td style="text-align: left">WorkerProcess (active / standby)</td> <td style="text-align: left">加载模型、执行推理、返回结果</td> </tr> </tbody> </table> <h3 id="实现说明">实现说明</h3> <p>这套系统的核心挑战是实现抢占。 所谓抢占，是指当在线任务到来时，如果所有 Worker 都已被离线任务占用，调度器需要主动中断某个正在运行的离线任务，腾出 Worker 来立即执行在线任务。由于离线任务本身对时延不敏感，这种中断是可以接受的，被中断的客户端会收到 preempted 状态码，由业务层决定是否重试。</p> <ul> <li> <p>抢占后的冷启动问题 抢占的执行很直接——kill 掉进程即可。真正的难点在于：kill 掉一个 Worker 之后，需要立刻有一个已经加载好模型的进程顶上来，而不是从零开始重新初始化。GPU 模型的加载通常需要数秒到数十秒，如果每次抢占都要等模型重新加载，在线任务的时延优势就荡然无存了。</p> </li> <li> <p>用 Standby 进程解决冷启动 为此，系统引入了 Standby Worker 的概念：在正常运行期间，后台始终维护一批预热好的备用进程。这些进程以 device=cpu 启动，提前完成 predictor 的初始化并将模型权重加载到内存，但不占用 GPU 资源，也不参与任务调度。 当抢占发生时，Standby Worker 被直接顶替到被杀 Worker 的位置，并在接到第一个任务时将模型从 CPU 迁移到 GPU（load_model(“cuda”)），整个替换过程的额外耗时仅是 CPU→GPU 的数据搬运，远比从零加载模型要快。</p> </li> <li> <p>Standby 的补建 Standby 进程是一次性消耗品——每次抢占都会消耗一个 Standby。为了维持随时可抢占的能力，调度器在完成一次抢占后会立即通知 ProcessManager 异步补建一个新的 Standby Worker。ProcessManager 在独立线程中完成新进程的启动和模型预热，完成后通过结果队列通知调度器，整个过程对调度主循环完全无阻塞。 这样，整个抢占链路形成了一个自我修复的闭环：抢占 → 消耗 Standby → 异步补建 → 恢复 Standby 储备，系统始终保持随时可响应在线任务的能力。</p> </li> </ul> <h3 id="关键设计决策">关键设计决策</h3> <p><strong>为什么用 ZMQ 而不是 multiprocessing.Queue？</strong></p> <p><code class="language-plaintext highlighter-rouge">multiprocessing.Queue</code> 只能在父子进程间使用，且模式单一。ZMQ 的 ROUTER/DEALER 模式天然支持：</p> <ul> <li>多客户端（Gunicorn 多线程）到单调度器的多对一通信</li> <li>调度器到多 Worker 的一对多派发</li> <li>基于 identity 的精确路由回包</li> </ul> <p><strong>为什么 Scheduler 运行在独立进程？</strong></p> <p>调度器持有所有 Worker 状态，是全局唯一的。把它放在独立进程里可以避免 Gunicorn fork 导致的状态不一致，也让调度逻辑与 HTTP 请求处理完全解耦。Gunicorn 可以有多个 Worker 进程，每个进程里的多个线程共享同一个 <code class="language-plaintext highlighter-rouge">SchedulerClient</code>，通过 ZMQ IPC 与 Scheduler 通信。</p> <p><strong>为什么用 spawn 而不是 fork？</strong></p> <p>CUDA 运行时不支持在 <code class="language-plaintext highlighter-rouge">fork</code> 出的子进程中重新初始化。由于 Worker 进程需要加载 GPU 模型，必须使用 <code class="language-plaintext highlighter-rouge">spawn</code> 方式创建子进程，确保每个 Worker 拥有独立的 CUDA 上下文。</p> <h2 id="工程实现">工程实现</h2> <h3 id="1-scheduler-调度器">1. Scheduler 调度器</h3> <p>Scheduler 是整个系统的核心，运行在独立进程中，负责：</p> <ul> <li>接收来自 Gunicorn 各线程的任务请求</li> <li>按优先级将任务放入 <code class="language-plaintext highlighter-rouge">high_queue</code> 或 <code class="language-plaintext highlighter-rouge">low_queue</code></li> <li>驱动调度循环，分配任务给空闲 Worker</li> <li>在无空闲 Worker 时触发抢占</li> <li>接收 Worker 返回的结果，路由回对应的客户端</li> </ul> <h4 id="启动流程">启动流程</h4> <p>Scheduler 通过 <code class="language-plaintext highlighter-rouge">_scheduler_entry()</code> 函数作为 <code class="language-plaintext highlighter-rouge">spawn</code> 进程的入口。在子进程内部，先兜底设置 <code class="language-plaintext highlighter-rouge">set_start_method("spawn")</code>，确保 Scheduler 内部再创建的 WorkerProcess 也使用 spawn：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">_scheduler_entry</span><span class="p">(</span><span class="n">worker_num</span><span class="p">,</span> <span class="n">predictor_config</span><span class="p">):</span>
    <span class="k">try</span><span class="p">:</span>
        <span class="n">mp</span><span class="p">.</span><span class="nf">set_start_method</span><span class="p">(</span><span class="sh">"</span><span class="s">spawn</span><span class="sh">"</span><span class="p">,</span> <span class="n">force</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="k">except</span> <span class="nb">RuntimeError</span><span class="p">:</span>
        <span class="k">pass</span>
    <span class="nc">Scheduler</span><span class="p">(</span><span class="n">worker_num</span><span class="o">=</span><span class="n">worker_num</span><span class="p">,</span> <span class="n">predictor_config</span><span class="o">=</span><span class="n">predictor_config</span><span class="p">).</span><span class="nf">run</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">start_scheduler</span><span class="p">(</span><span class="n">worker_num</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">predictor_config</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
    <span class="n">ctx</span> <span class="o">=</span> <span class="n">mp</span><span class="p">.</span><span class="nf">get_context</span><span class="p">(</span><span class="sh">"</span><span class="s">spawn</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">p</span> <span class="o">=</span> <span class="n">ctx</span><span class="p">.</span><span class="nc">Process</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">_scheduler_entry</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">worker_num</span><span class="p">,</span> <span class="n">predictor_config</span><span class="p">),</span> <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Scheduler</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">p</span><span class="p">.</span><span class="n">daemon</span> <span class="o">=</span> <span class="bp">False</span>
    <span class="n">p</span><span class="p">.</span><span class="nf">start</span><span class="p">()</span>
    <span class="k">return</span> <span class="nc">SchedulerHandle</span><span class="p">(</span><span class="n">process</span><span class="o">=</span><span class="n">p</span><span class="p">)</span>
</code></pre></div></div> <p>注意这里不能写成 <code class="language-plaintext highlighter-rouge">Process(target=scheduler.run)</code>——因为 spawn 会尝试 pickle <code class="language-plaintext highlighter-rouge">Scheduler</code> 实例，而它内部包含 <code class="language-plaintext highlighter-rouge">queue.Queue</code>（不可 pickle），所以必须在子进程入口函数里再构造。</p> <h4 id="zmq-双-router-架构">ZMQ 双 ROUTER 架构</h4> <p>Scheduler 持有两个 ZMQ ROUTER socket：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">_init_zmq_sockets</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="n">ctx</span> <span class="o">=</span> <span class="n">zmq</span><span class="p">.</span><span class="nc">Context</span><span class="p">()</span>
    <span class="c1"># 面向 Gunicorn 客户端
</span>    <span class="n">self</span><span class="p">.</span><span class="n">router</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">ctx</span><span class="p">.</span><span class="nf">socket</span><span class="p">(</span><span class="n">zmq</span><span class="p">.</span><span class="n">ROUTER</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="n">router</span><span class="p">.</span><span class="nf">bind</span><span class="p">(</span><span class="sh">"</span><span class="s">ipc:///tmp/scheduler_router.sock</span><span class="sh">"</span><span class="p">)</span>
    <span class="c1"># 面向 Worker 进程
</span>    <span class="n">self</span><span class="p">.</span><span class="n">worker_router</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">ctx</span><span class="p">.</span><span class="nf">socket</span><span class="p">(</span><span class="n">zmq</span><span class="p">.</span><span class="n">ROUTER</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="n">worker_router</span><span class="p">.</span><span class="nf">bind</span><span class="p">(</span><span class="sh">"</span><span class="s">ipc:///tmp/worker_router.sock</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">self</span><span class="p">.</span><span class="n">poller</span> <span class="o">=</span> <span class="n">zmq</span><span class="p">.</span><span class="nc">Poller</span><span class="p">()</span>
    <span class="n">self</span><span class="p">.</span><span class="n">poller</span><span class="p">.</span><span class="nf">register</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">router</span><span class="p">,</span> <span class="n">zmq</span><span class="p">.</span><span class="n">POLLIN</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="n">poller</span><span class="p">.</span><span class="nf">register</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">worker_router</span><span class="p">,</span> <span class="n">zmq</span><span class="p">.</span><span class="n">POLLIN</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">router</code>：接收 <code class="language-plaintext highlighter-rouge">SchedulerClient</code>（DEALER）发来的任务请求，ROUTER 会自动记录每个 DEALER 的 identity，回包时按 identity 路由。</li> <li><code class="language-plaintext highlighter-rouge">worker_router</code>：向 <code class="language-plaintext highlighter-rouge">WorkerProcess</code>（DEALER）派发任务和接收结果。</li> </ul> <p>使用 IPC 而非 TCP，是因为所有组件在同一台机器上运行，IPC 比 TCP 少了网络协议栈的开销。</p> <h4 id="主循环">主循环</h4> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">_start_workers</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">worker_num</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">_start_process_manager</span><span class="p">()</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">_init_zmq_sockets</span><span class="p">()</span>

    <span class="k">while</span> <span class="n">self</span><span class="p">.</span><span class="n">_is_running</span><span class="p">:</span>
        <span class="n">events</span> <span class="o">=</span> <span class="nf">dict</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">poller</span><span class="p">.</span><span class="nf">poll</span><span class="p">(</span><span class="n">timeout</span><span class="o">=</span><span class="mi">100</span><span class="p">))</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">worker_router</span> <span class="ow">in</span> <span class="n">events</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">_handle_worker_result</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">router</span> <span class="ow">in</span> <span class="n">events</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">_handle_client_request</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">_schedule</span><span class="p">()</span>
</code></pre></div></div> <p>主循环做三件事：</p> <ol> <li>收 Worker 的结果（有结果就路由回客户端）</li> <li>收客户端的新请求（按优先级入队）</li> <li>执行调度（分配任务 / 抢占）</li> </ol> <p><code class="language-plaintext highlighter-rouge">poller.poll(timeout=100)</code> 是事件驱动的核心：100ms 超时确保即使没有事件也能周期性执行 <code class="language-plaintext highlighter-rouge">_schedule()</code>，不会死等。</p> <h4 id="调度策略">调度策略</h4> <p><code class="language-plaintext highlighter-rouge">_schedule()</code> 分三个阶段：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">_schedule</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">_check_background_workers</span><span class="p">()</span>

    <span class="c1"># 阶段一：高优任务分配给空闲 Worker
</span>    <span class="k">while</span> <span class="ow">not</span> <span class="n">self</span><span class="p">.</span><span class="n">high_queue</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
        <span class="n">worker_id</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_get_idle_worker</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">worker_id</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">_assign</span><span class="p">(</span><span class="n">worker_id</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">high_queue</span><span class="p">.</span><span class="nf">get</span><span class="p">())</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="k">break</span>

    <span class="c1"># 阶段二：高优任务仍有剩余，抢占低优 Worker
</span>    <span class="k">while</span> <span class="ow">not</span> <span class="n">self</span><span class="p">.</span><span class="n">high_queue</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
        <span class="n">killed_id</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_preempt_low_priority</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">killed_id</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">_handle_kill_low_priority</span><span class="p">(</span><span class="n">killed_id</span><span class="p">)</span>
            <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">standby_workers</span><span class="p">)</span> <span class="o">&gt;=</span> <span class="mi">1</span><span class="p">:</span>
                <span class="n">self</span><span class="p">.</span><span class="nf">_arrange_background_workers</span><span class="p">(</span><span class="n">killed_id</span><span class="p">)</span>
                <span class="n">self</span><span class="p">.</span><span class="nf">_assign</span><span class="p">(</span><span class="n">killed_id</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">high_queue</span><span class="p">.</span><span class="nf">get</span><span class="p">())</span>
            <span class="c1"># 通知 ProcessManager 异步补建 standby
</span>            <span class="n">self</span><span class="p">.</span><span class="n">process_manager_task_queue</span><span class="p">.</span><span class="nf">put</span><span class="p">({</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">worker_id</span><span class="sh">"</span><span class="p">:</span> <span class="n">killed_id</span><span class="p">})</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="k">break</span>

    <span class="c1"># 阶段三：没有高优任务时，分配低优任务
</span>    <span class="k">while</span> <span class="ow">not</span> <span class="n">self</span><span class="p">.</span><span class="n">low_queue</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
        <span class="n">worker_id</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_get_idle_worker</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">worker_id</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">break</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">_assign</span><span class="p">(</span><span class="n">worker_id</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">low_queue</span><span class="p">.</span><span class="nf">get</span><span class="p">())</span>
</code></pre></div></div> <h3 id="2-抢占机制">2. 抢占机制</h3> <p>抢占是这套调度系统最关键的能力。当高优任务到达但所有 Worker 都在忙时，调度器会：</p> <ol> <li><strong>选择一个正在执行低优任务的 Worker</strong></li> <li><strong>杀掉该 Worker 进程</strong>（先 SIGTERM，等待短暂时间后 SIGKILL）</li> <li><strong>向被抢占任务的客户端返回 preempted 状态</strong></li> <li><strong>用预热好的 standby Worker 替换被杀的 Worker</strong></li> <li><strong>将高优任务分配给新 Worker</strong></li> <li><strong>异步通知 ProcessManager 补建新的 standby Worker</strong></li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">_kill_worker</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">worker_id</span><span class="p">):</span>
    <span class="n">w</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">active_workers</span><span class="p">[</span><span class="n">worker_id</span><span class="p">]</span>
    <span class="n">p</span> <span class="o">=</span> <span class="n">w</span><span class="p">[</span><span class="sh">"</span><span class="s">process</span><span class="sh">"</span><span class="p">]</span>
    <span class="k">try</span><span class="p">:</span>
        <span class="n">p</span><span class="p">.</span><span class="nf">stop</span><span class="p">()</span>
        <span class="n">os</span><span class="p">.</span><span class="nf">kill</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="n">pid</span><span class="p">,</span> <span class="n">signal</span><span class="p">.</span><span class="n">SIGTERM</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">5</span><span class="p">):</span>
            <span class="n">res</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="nf">waitpid</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="n">pid</span><span class="p">,</span> <span class="n">os</span><span class="p">.</span><span class="n">WNOHANG</span><span class="p">)</span>
            <span class="k">if</span> <span class="n">res</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">:</span>
                <span class="k">break</span>
            <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.001</span><span class="p">)</span>
        <span class="c1"># 优雅关闭失败，强制杀死
</span>        <span class="n">os</span><span class="p">.</span><span class="nf">kill</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="n">pid</span><span class="p">,</span> <span class="n">signal</span><span class="p">.</span><span class="n">SIGKILL</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">5</span><span class="p">):</span>
            <span class="n">pid</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="nf">waitpid</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="n">pid</span><span class="p">,</span> <span class="n">os</span><span class="p">.</span><span class="n">WNOHANG</span><span class="p">)</span>
            <span class="k">if</span> <span class="n">pid</span> <span class="o">==</span> <span class="n">p</span><span class="p">.</span><span class="n">pid</span><span class="p">:</span>
                <span class="k">return</span>
            <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.001</span><span class="p">)</span>
    <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
        <span class="n">os</span><span class="p">.</span><span class="nf">kill</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="n">pid</span><span class="p">,</span> <span class="n">signal</span><span class="p">.</span><span class="n">SIGKILL</span><span class="p">)</span>
</code></pre></div></div> <p>被抢占的客户端会收到明确的状态码：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">msg</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">task_id</span><span class="sh">"</span><span class="p">:</span> <span class="n">task_id</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">preempted</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">result</span><span class="sh">"</span><span class="p">:</span> <span class="bp">None</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">error_msg</span><span class="sh">"</span><span class="p">:</span> <span class="bp">None</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="3-worker-进程">3. Worker 进程</h3> <p>WorkerProcess 是实际执行推理的进程。每个 Worker 在启动时：</p> <ol> <li>根据 <code class="language-plaintext highlighter-rouge">predictor_config</code> 在子进程内构造 predictor 实例</li> <li>调用 <code class="language-plaintext highlighter-rouge">predictor.load_model(device)</code> 加载模型到指定设备</li> <li>初始化 ZMQ DEALER socket 并连接到 Scheduler 的 worker_router</li> <li>进入事件循环，等待任务、执行、回包</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">WorkerProcess</span><span class="p">(</span><span class="n">Process</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">run</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">_init_predictor</span><span class="p">()</span>       <span class="c1"># 子进程内构造 predictor
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">_init_zmq_sockets</span><span class="p">()</span>     <span class="c1"># 子进程内初始化 ZMQ
</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">predictor</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">predictor</span><span class="p">.</span><span class="nf">load_model</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>

        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">ready_event</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">ready_event</span><span class="p">.</span><span class="nf">set</span><span class="p">()</span>   <span class="c1"># 通知 ProcessManager 已就绪
</span>
        <span class="k">while</span> <span class="n">self</span><span class="p">.</span><span class="n">_running</span><span class="p">:</span>
            <span class="n">socks</span> <span class="o">=</span> <span class="nf">dict</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">poller</span><span class="p">.</span><span class="nf">poll</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">timeout</span> <span class="o">*</span> <span class="mi">1000</span><span class="p">))</span>
            <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">worker_socket</span> <span class="ow">in</span> <span class="n">socks</span><span class="p">:</span>
                <span class="n">_</span><span class="p">,</span> <span class="n">reply</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">worker_socket</span><span class="p">.</span><span class="nf">recv_multipart</span><span class="p">()</span>
                <span class="n">task</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">loads</span><span class="p">(</span><span class="n">reply</span><span class="p">.</span><span class="nf">decode</span><span class="p">())</span>
                <span class="k">try</span><span class="p">:</span>
                    <span class="n">result</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">run_task</span><span class="p">(</span><span class="n">task</span><span class="p">)</span>
                    <span class="n">self</span><span class="p">.</span><span class="n">worker_socket</span><span class="p">.</span><span class="nf">send_multipart</span><span class="p">([</span><span class="sa">b</span><span class="sh">""</span><span class="p">,</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">(</span><span class="n">result</span><span class="p">).</span><span class="nf">encode</span><span class="p">()])</span>
                <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
                    <span class="n">error_result</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">failed</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">error_msg</span><span class="sh">"</span><span class="p">:</span> <span class="nf">str</span><span class="p">(</span><span class="n">e</span><span class="p">),</span> <span class="p">...}</span>
                    <span class="n">self</span><span class="p">.</span><span class="n">worker_socket</span><span class="p">.</span><span class="nf">send_multipart</span><span class="p">([</span><span class="sa">b</span><span class="sh">""</span><span class="p">,</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">(</span><span class="n">error_result</span><span class="p">).</span><span class="nf">encode</span><span class="p">()])</span>
</code></pre></div></div> <h4 id="active-vs-standby">Active vs Standby</h4> <p>系统启动时会同时创建两组 Worker：</p> <table> <thead> <tr> <th style="text-align: left">类型</th> <th style="text-align: left">设备</th> <th style="text-align: left">用途</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Active Worker</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">device=cuda</code></td> <td style="text-align: left">正常接收并执行任务</td> </tr> <tr> <td style="text-align: left">Standby Worker</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">device=cpu</code></td> <td style="text-align: left">预热 predictor（仅 CPU），不参与调度，等待抢占时切换</td> </tr> </tbody> </table> <p>Standby Worker 在 CPU 上预先加载了模型，当发生抢占替换时，只需要将模型从 CPU 搬到 GPU（<code class="language-plaintext highlighter-rouge">load_model("cuda")</code>），比从零启动一个新进程快得多。</p> <h3 id="4-processmanager">4. ProcessManager</h3> <p>ProcessManager 是一个后台线程，负责异步补建 standby Worker。当 Scheduler 抢占消耗了一个 standby Worker 后，会通过 <code class="language-plaintext highlighter-rouge">process_manager_task_queue</code> 发送补建请求：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ProcessManager</span><span class="p">(</span><span class="n">Thread</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">run</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="k">while</span> <span class="n">self</span><span class="p">.</span><span class="n">_running</span><span class="p">:</span>
            <span class="n">task</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">task_queue</span><span class="p">.</span><span class="nf">get</span><span class="p">()</span>
            <span class="k">if</span> <span class="n">task</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">)</span> <span class="o">==</span> <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">:</span>
                <span class="n">worker_id</span> <span class="o">=</span> <span class="n">task</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">worker_id</span><span class="sh">"</span><span class="p">)</span>
                <span class="n">ready_event</span> <span class="o">=</span> <span class="nc">Event</span><span class="p">()</span>
                <span class="n">p</span> <span class="o">=</span> <span class="nc">WorkerProcess</span><span class="p">(</span><span class="n">worker_id</span><span class="o">=</span><span class="n">worker_id</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cpu</span><span class="sh">"</span><span class="p">,</span>
                                  <span class="n">ready_event</span><span class="o">=</span><span class="n">ready_event</span><span class="p">,</span>
                                  <span class="n">predictor_config</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">predictor_config</span><span class="p">)</span>
                <span class="n">p</span><span class="p">.</span><span class="nf">start</span><span class="p">()</span>
                <span class="k">if</span> <span class="n">ready_event</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="n">timeout</span><span class="o">=</span><span class="mi">120</span><span class="p">):</span>
                    <span class="n">self</span><span class="p">.</span><span class="n">result_queue</span><span class="p">.</span><span class="nf">put</span><span class="p">({</span>
                        <span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">worker_ready</span><span class="sh">"</span><span class="p">,</span>
                        <span class="sh">"</span><span class="s">worker_id</span><span class="sh">"</span><span class="p">:</span> <span class="n">worker_id</span><span class="p">,</span>
                        <span class="sh">"</span><span class="s">process</span><span class="sh">"</span><span class="p">:</span> <span class="n">p</span>
                    <span class="p">})</span>
</code></pre></div></div> <p>ProcessManager 使用 <code class="language-plaintext highlighter-rouge">Event</code> 机制等待新 Worker 加载完成。Scheduler 在每轮 <code class="language-plaintext highlighter-rouge">_schedule()</code> 开头调用 <code class="language-plaintext highlighter-rouge">_check_background_workers()</code> 非阻塞地检查是否有新 standby 就绪。</p> <h3 id="5-schedulerclient">5. SchedulerClient</h3> <p>SchedulerClient 运行在 Gunicorn Worker 进程中，负责将业务请求通过 ZMQ 发送给 Scheduler，并同步等待结果。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">SchedulerClient</span><span class="p">:</span>
    <span class="n">_instance</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="n">_lock</span> <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Lock</span><span class="p">()</span>
    <span class="n">_thread_local</span> <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nf">local</span><span class="p">()</span>

    <span class="nd">@property</span>
    <span class="k">def</span> <span class="nf">socket</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="nf">hasattr</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">_thread_local</span><span class="p">,</span> <span class="sh">'</span><span class="s">socket</span><span class="sh">'</span><span class="p">):</span>
            <span class="n">self</span><span class="p">.</span><span class="n">_thread_local</span><span class="p">.</span><span class="n">socket</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">ctx</span><span class="p">.</span><span class="nf">socket</span><span class="p">(</span><span class="n">zmq</span><span class="p">.</span><span class="n">DEALER</span><span class="p">)</span>
            <span class="n">identity</span> <span class="o">=</span> <span class="nf">str</span><span class="p">(</span><span class="n">uuid</span><span class="p">.</span><span class="nf">uuid4</span><span class="p">()).</span><span class="nf">encode</span><span class="p">()</span>
            <span class="n">self</span><span class="p">.</span><span class="n">_thread_local</span><span class="p">.</span><span class="n">socket</span><span class="p">.</span><span class="nf">setsockopt</span><span class="p">(</span><span class="n">zmq</span><span class="p">.</span><span class="n">IDENTITY</span><span class="p">,</span> <span class="n">identity</span><span class="p">)</span>
            <span class="n">self</span><span class="p">.</span><span class="n">_thread_local</span><span class="p">.</span><span class="n">socket</span><span class="p">.</span><span class="nf">connect</span><span class="p">(</span><span class="n">SCHEDULER_ROUTER_ADDR</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">_thread_local</span><span class="p">.</span><span class="n">socket</span>

    <span class="k">def</span> <span class="nf">submit_task</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">task_id</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="n">payload</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="n">priority</span><span class="o">=</span><span class="sh">"</span><span class="s">high</span><span class="sh">"</span><span class="p">,</span> <span class="n">timeout</span><span class="o">=</span><span class="mf">60.0</span><span class="p">):</span>
        <span class="n">task_msg</span> <span class="o">=</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">task_id</span><span class="sh">"</span><span class="p">:</span> <span class="n">task_id</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">priority</span><span class="sh">"</span><span class="p">:</span> <span class="n">priority</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">payload</span><span class="sh">"</span><span class="p">:</span> <span class="n">payload</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">submit_ts</span><span class="sh">"</span><span class="p">:</span> <span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">(),</span>
            <span class="sh">"</span><span class="s">timeout</span><span class="sh">"</span><span class="p">:</span> <span class="n">timeout</span>
        <span class="p">}</span>
        <span class="n">self</span><span class="p">.</span><span class="n">socket</span><span class="p">.</span><span class="nf">send_multipart</span><span class="p">([</span><span class="sa">b</span><span class="sh">""</span><span class="p">,</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">(</span><span class="n">task_msg</span><span class="p">).</span><span class="nf">encode</span><span class="p">()])</span>
        <span class="n">socks</span> <span class="o">=</span> <span class="nf">dict</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">poller</span><span class="p">.</span><span class="nf">poll</span><span class="p">(</span><span class="n">timeout</span> <span class="o">*</span> <span class="mi">1000</span><span class="p">))</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">socket</span> <span class="ow">in</span> <span class="n">socks</span><span class="p">:</span>
            <span class="n">_</span><span class="p">,</span> <span class="n">reply</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">socket</span><span class="p">.</span><span class="nf">recv_multipart</span><span class="p">()</span>
            <span class="k">return</span> <span class="n">json</span><span class="p">.</span><span class="nf">loads</span><span class="p">(</span><span class="n">reply</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="k">raise</span> <span class="nc">TimeoutError</span><span class="p">(</span><span class="sh">"</span><span class="s">Scheduler response timeout</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>关键设计点：</p> <ul> <li><strong>单例模式</strong>：每个 Gunicorn Worker 进程只有一个 <code class="language-plaintext highlighter-rouge">SchedulerClient</code> 实例，共享同一个 <code class="language-plaintext highlighter-rouge">zmq.Context</code>。</li> <li><strong>线程局部 Socket</strong>：Gunicorn 的 gthread worker 是多线程的，ZMQ Socket 不是线程安全的，所以每个线程持有自己的 DEALER socket。<code class="language-plaintext highlighter-rouge">zmq.Context</code> 是线程安全的，可以跨线程共享。</li> <li><strong>DEALER + 空帧</strong>：DEALER 发消息时需要加一个空帧 <code class="language-plaintext highlighter-rouge">b""</code>，以兼容 ROUTER 端期望的 <code class="language-plaintext highlighter-rouge">[identity, empty, payload]</code> 三帧结构。</li> </ul> <h2 id="写在最后">写在最后</h2> <p>为什么要记录这个任务的实现，因为最近在vllm的整体架构，我发现vllm的整体的进程架构和我自己实现的这个功能很类似，在实现之前我还没开始看vllm的整体架构，看完却发现似曾相识，这样理解起来vllm的架构相对来说就轻松一些了。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/scheduler/vllm-480.webp 480w,/assets/img/posts/scheduler/vllm-800.webp 800w,/assets/img/posts/scheduler/vllm-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/scheduler/vllm.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure>]]></content><author><name></name></author><category term="programming"/><category term="computer"/><category term="python"/><summary type="html"><![CDATA[记录python中实现多进程任务调度的工程实现,其中的进程调度架构类似vllm中的实现。]]></summary></entry><entry><title type="html">最常用的 Linux 命令的 Windows 对应命令</title><link href="https://damonsj.github.io/programming/2026/04/03/linux-commands-in-windows.html" rel="alternate" type="text/html" title="最常用的 Linux 命令的 Windows 对应命令"/><published>2026-04-03T01:10:00+08:00</published><updated>2026-04-03T01:10:00+08:00</updated><id>https://damonsj.github.io/programming/2026/04/03/linux-commands-in-windows</id><content type="html" xml:base="https://damonsj.github.io/programming/2026/04/03/linux-commands-in-windows.html"><![CDATA[<p>平时在 Linux 和 Windows 之间切换时，最头疼的通常不是“有没有工具”，而是“同一件事在另一边该怎么敲命令”。<br/> 这篇做一个常用场景速查，先给出 Windows <code class="language-plaintext highlighter-rouge">cmd</code> 对照，再补充更原生的 PowerShell 写法。</p> <h2 id="一常用命令速查表">一、常用命令速查表</h2> <table> <thead> <tr> <th>Linux</th> <th>Windows (cmd)</th> <th>Windows (PowerShell)</th> <th>用途</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">lsof -i \| grep https</code></td> <td><code class="language-plaintext highlighter-rouge">netstat -n -a \| findstr "https"</code></td> <td><code class="language-plaintext highlighter-rouge">netstat -n -a \| Select-String "https"</code></td> <td>过滤网络连接信息</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ssh root@remote "tcpdump -s 0 -U -n -w - -i eth0 not port 22" \| wireshark -k -i -</code></td> <td><code class="language-plaintext highlighter-rouge">ssh root@remote "tcpdump -s 0 -U -n -w - -i eth0 not port 22" \| "C:\Program Files\Wireshark\wireshark.exe" -k -i -</code></td> <td><code class="language-plaintext highlighter-rouge">ssh root@remote "tcpdump -s 0 -U -n -w - -i eth0 not port 22" \| "C:\Program Files\Wireshark\wireshark.exe" -k -i -</code></td> <td>远端抓包并本地 Wireshark 实时看流量</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">netstat -tulpn</code></td> <td><code class="language-plaintext highlighter-rouge">netstat -ano</code></td> <td><code class="language-plaintext highlighter-rouge">Get-NetTCPConnection -State Listen \| Select-Object LocalAddress,LocalPort,OwningProcess</code></td> <td>查看端口监听与 PID</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">cat filename.txt</code></td> <td><code class="language-plaintext highlighter-rouge">type filename.txt</code></td> <td><code class="language-plaintext highlighter-rouge">Get-Content filename.txt</code></td> <td>在终端直接查看文件内容</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ls -la</code></td> <td><code class="language-plaintext highlighter-rouge">dir /a</code></td> <td><code class="language-plaintext highlighter-rouge">Get-ChildItem -Force</code></td> <td>列出目录内容（含隐藏文件）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">find / -name "config.txt"</code></td> <td><code class="language-plaintext highlighter-rouge">dir config.txt /s</code></td> <td><code class="language-plaintext highlighter-rouge">Get-ChildItem -Path . -Filter "config.txt" -Recurse -File</code></td> <td>递归查找文件</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ifconfig</code> / <code class="language-plaintext highlighter-rouge">ip a</code></td> <td><code class="language-plaintext highlighter-rouge">ipconfig /all</code></td> <td><code class="language-plaintext highlighter-rouge">Get-NetIPConfiguration</code></td> <td>查看网卡和 IP 配置</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">top</code> / <code class="language-plaintext highlighter-rouge">ps aux</code></td> <td><code class="language-plaintext highlighter-rouge">tasklist</code></td> <td><code class="language-plaintext highlighter-rouge">Get-Process</code></td> <td>查看进程列表</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kill -9 1234</code></td> <td><code class="language-plaintext highlighter-rouge">taskkill /F /PID 1234</code></td> <td><code class="language-plaintext highlighter-rouge">Stop-Process -Id 1234 -Force</code></td> <td>按 PID 强制结束进程</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">traceroute google.com</code></td> <td><code class="language-plaintext highlighter-rouge">tracert google.com</code></td> <td><code class="language-plaintext highlighter-rouge">Test-NetConnection google.com -TraceRoute</code></td> <td>路由追踪</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">clear</code></td> <td><code class="language-plaintext highlighter-rouge">cls</code></td> <td><code class="language-plaintext highlighter-rouge">Clear-Host</code></td> <td>清屏</td> </tr> </tbody> </table> <h2 id="二按场景展开">二、按场景展开</h2> <h3 id="1-过滤命令输出">1) 过滤命令输出</h3> <p>Linux 常用 <code class="language-plaintext highlighter-rouge">grep</code>，在 <code class="language-plaintext highlighter-rouge">cmd</code> 里一般用 <code class="language-plaintext highlighter-rouge">findstr</code>：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>netstat <span class="nt">-n</span> <span class="nt">-a</span> | findstr <span class="s2">"https"</span>
</code></pre></div></div> <p>注意关键字建议带双引号，避免空格或特殊字符导致匹配异常。</p> <h3 id="2-远端抓包并在本机-wireshark-实时查看">2) 远端抓包并在本机 Wireshark 实时查看</h3> <p>如果你有 Linux 服务器权限，可以直接把 <code class="language-plaintext highlighter-rouge">tcpdump</code> 输出通过 SSH 管道传给本地 Wireshark：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh root@remote-linux <span class="s2">"tcpdump -s 0 -U -n -w - -i eth0 not port 22"</span> | <span class="s2">"C:</span><span class="se">\P</span><span class="s2">rogram Files</span><span class="se">\W</span><span class="s2">ireshark</span><span class="se">\w</span><span class="s2">ireshark.exe"</span> <span class="nt">-k</span> <span class="nt">-i</span> -
</code></pre></div></div> <p>这里路径有空格，所以需要用双引号包住 <code class="language-plaintext highlighter-rouge">wireshark.exe</code> 路径。</p> <h3 id="3-查看端口监听对应进程">3) 查看端口监听对应进程</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>netstat <span class="nt">-ano</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">-o</code> 会带出 PID，拿到 PID 后可以在任务管理器或配合 <code class="language-plaintext highlighter-rouge">tasklist</code> 进一步定位进程。</p> <h3 id="4-递归查找文件">4) 递归查找文件</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">dir </span>config.txt /s
</code></pre></div></div> <p>这会在当前目录（及其子目录）递归查找文件。<br/> 如果想从某个盘根目录开始，可先切到该盘符再执行（例如 <code class="language-plaintext highlighter-rouge">C:</code>）。</p> <h2 id="三powershell-常用一行命令">三、PowerShell 常用一行命令</h2> <p>如果你主要使用 PowerShell，可以直接记这几条：</p> <table> <thead> <tr> <th>命令</th> <th>一句话说明</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Get-NetTCPConnection -State Listen \| Select-Object LocalAddress,LocalPort,OwningProcess</code></td> <td>查看当前监听端口及对应进程 PID。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Get-ChildItem -Path . -Filter "*.log" -Recurse -File</code></td> <td>递归查找当前目录下所有 <code class="language-plaintext highlighter-rouge">.log</code> 文件。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Get-Process \| Sort-Object CPU -Descending \| Select-Object -First 10</code></td> <td>按 CPU 使用量排序并显示前 10 个进程。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Stop-Process -Id 1234 -Force</code></td> <td>强制结束 PID 为 <code class="language-plaintext highlighter-rouge">1234</code> 的进程。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Test-NetConnection github.com -TraceRoute</code></td> <td>测试到目标主机的连通性并输出路由路径。</td> </tr> </tbody> </table> <h2 id="四一些实用提醒">四、一些实用提醒</h2> <ul> <li>Windows 10/11 默认已内置 OpenSSH 客户端，可直接使用 <code class="language-plaintext highlighter-rouge">ssh</code>。</li> <li><code class="language-plaintext highlighter-rouge">cmd</code> 与 PowerShell 命令生态不同，前者偏“经典命令”，后者偏“对象管道”。</li> <li>网络排查场景常用组合是：<code class="language-plaintext highlighter-rouge">netstat -ano</code> + <code class="language-plaintext highlighter-rouge">tasklist</code> + <code class="language-plaintext highlighter-rouge">taskkill</code>。</li> </ul> <h2 id="参考">参考</h2> <ul> <li>原文：<a href="https://techkettle.blogspot.com/2026/04/the-windows-equivalents-of-most-used.html">The Windows equivalents of the most used Linux commands</a></li> </ul>]]></content><author><name></name></author><category term="programming"/><category term="computer"/><category term="转载"/><summary type="html"><![CDATA[记录最常用的 Linux 命令的 Windows 对应命令。]]></summary></entry><entry><title type="html">PyZMQ的简介</title><link href="https://damonsj.github.io/programming/2026/01/18/pyzmq-introduction.html" rel="alternate" type="text/html" title="PyZMQ的简介"/><published>2026-01-18T01:10:00+08:00</published><updated>2026-01-18T01:10:00+08:00</updated><id>https://damonsj.github.io/programming/2026/01/18/pyzmq-introduction</id><content type="html" xml:base="https://damonsj.github.io/programming/2026/01/18/pyzmq-introduction.html"><![CDATA[<h2 id="一从-socket-谈起为什么要进化">一、从 Socket 谈起：为什么要进化？</h2> <h3 id="1-什么是socket">1. 什么是socket？</h3> <p>我最早接触 ZMQ，是在 sglang 的代码里看到它被用来做进程间通信。当时的理解很模糊：</p> <blockquote class="block-tip"> <p>ZMQ 好像是个用来进程通信的库。</p> </blockquote> <p>真正去调研之后才发现，ZMQ 并不是“绕过” Socket，而是构建在 Socket 之上： 它可以在进程内、进程间，甚至跨机器（TCP）进行消息传递，而底层依然是我们熟悉的网络通信机制。</p> <p>于是问题就回到了一个老问题上： Socket 到底是什么？</p> <p>简单来说，Socket（套接字）是应用程序访问 TCP/IP 协议栈的统一接口。 它不是协议本身，而是操作系统提供的一层抽象 API，用来屏蔽底层复杂的网络实现。</p> <p>你可以把它理解为一个“插口”：</p> <ul> <li> <p>一端连着应用程序</p> </li> <li> <p>另一端连着 TCP / UDP 协议栈</p> </li> <li> <p>应用只需要通过 Socket 读写数据，而不需要关心包是怎么在网络中传输的</p> </li> </ul> <p>从设计角度看，Socket 本质上是一个门面（Facade）： 它把复杂的 TCP/IP 细节隐藏在一组统一的接口之后，让应用层可以用相对简单的方式进行网络通信。</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/pyzmq/socket-480.webp 480w,/assets/img/posts/pyzmq/socket-800.webp 800w,/assets/img/posts/pyzmq/socket-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/pyzmq/socket.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Fig. 1. socket层级图. </div> <h3 id="2-有什么痛点">2. 有什么痛点？</h3> <p>如果只做简单的点对点通信，原生 Socket 完全够用。 但一旦系统开始变复杂，问题就会迅速暴露出来。</p> <h4 id="1-消息边界问题粘包--半包">1. 消息边界问题（粘包 / 半包）</h4> <p>TCP 是面向字节流的协议，它并不知道什么是“一条完整的业务消息”。 你发送的一条”完整消息”可能被拆成多个 TCP 包，或者多条消息粘在一起被一次性接收。这意味着你必须手动实现消息帧的分割与重组逻辑，例如添加长度前缀或特殊分隔符。</p> <h4 id="2-连接与重连逻辑">2. 连接与重连逻辑</h4> <p>Socket 本身是脆弱的，如果服务端重启或网络抖动，客户端的 Socket 连接会断开。你需要手写自动重连、超时重传、心跳检测等一整套容错机制，代码复杂且容易出错。而这些代码，和业务逻辑几乎没有任何关系。</p> <h4 id="3-并发与扩展性成本高">3. 并发与扩展性成本高</h4> <p>传统的同步阻塞 Socket（一个连接一个线程）在高并发场景下性能低下。改用非阻塞 I/O 或多路复用（如 select/epoll）又会让代码复杂度急剧上升，需要精心设计事件循环和状态机。 这时你会发现： 通信本身反而成了系统中最难维护的部分。</p> <h4 id="4-缺乏高层消息模式">4. 缺乏高层消息模式</h4> <p>Socket 只提供点对点的双向通道，如果要实现发布-订阅、请求-响应、任务分发等常见模式,你需要从零开始设计协议和拓扑结构。</p> <h2 id="二zmq不仅仅是-socket">二、ZMQ：不仅仅是 Socket</h2> <h3 id="1-zmq是什么">1. ZMQ是什么？</h3> <p>官方定义为：</p> <blockquote class="block-tip"> <p>ZeroMQ (also known as ØMQ, 0MQ, or zmq) looks like an embeddable networking library but acts like a concurrency framework. It gives you sockets that carry atomic messages across various transports like in-process, inter-process, TCP, and multicast. You can connect sockets N-to-N with patterns like fan-out, pub-sub, task distribution, and request-reply. It’s fast enough to be the fabric for clustered products. Its asynchronous I/O model gives you scalable multicore applications, built as asynchronous message-processing tasks. It has a score of language APIs and runs on most operating systems.</p> </blockquote> <p>也就是说<a href="https://zeromq.org/">ZeroMQ</a>可嵌入的网络库，但实际上更像是一个并发框架。它提供套接字，支持跨进程内、进程间、TCP 和多播等多种传输方式传递原子消息。您可以利用扇出、发布/订阅、任务分发和请求/响应等模式，实现多对多套接字连接。它的速度足以支撑集群产品的构建。其异步 I/O 模型支持可扩展的多核应用程序，这些应用程序以异步消息处理任务的形式构建。它拥有丰富的语言 API，并且可以在大多数操作系统上运行。</p> <p>ZMQ 并不取代 Socket，它是建立在 Socket 之上的高层抽象，把很多重复、容易出错的通信逻辑封装起来，同时提供了<strong>“消息模式”</strong>来约束和指导你的通信结构。</p> <h3 id="2-zmq解决了哪些问题">2. ZMQ解决了哪些问题？</h3> <p>针对我们在原生 Socket 中遇到的痛点，ZMQ 提供了如下的解决方案：</p> <h4 id="1-解决消息边界从字节流到离散消息">1. 解决消息边界：从“字节流”到“离散消息”</h4> <p>ZMQ 彻底终结了“粘包”噩梦。在 ZMQ 中，你发送的是原子级的消息块（Message Frames）。 在 TCP 之上，ZMQ 把消息拆成“帧”，保证你发送的消息在对端完整接收。你不必再自己设计长度字段、分隔符、缓冲区状态机。</p> <h4 id="2-解决可靠性静默重连与拓扑无关">2. 解决可靠性：静默重连与拓扑无关</h4> <p>自动重连：如果你的 Worker 进程崩溃重启，Scheduler 甚至不需要感知。ZMQ 会在后台自动排队消息并不断尝试重连，直到链路恢复。</p> <p>启动顺序无关：你可以先启动 Client，再启动 Server。Client 会静默等待直到连接建立，不会抛出任何报错。这让分布式系统的启动脚本变得无比简单。</p> <h4 id="3-解决并发难题全异步的后台-io">3. 解决并发难题：全异步的后台 IO</h4> <p>原生 Socket 的并发往往意味着复杂的 select/epoll。</p> <p>什么是select？ 意思是对所有的socket轮询，哪个有数据就通知哪个。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ready_sockets = select(read_list)
for s in ready_sockets:
    s.recv()
</code></pre></div></div> <p>什么是epoll？ epoll 是 Linux 为解决高并发而设计的进阶工具（也是现在高性能服务器如 Nginx、Redis 的底层基石）。把 socket 注册到 epoll，epoll 在内核中维护监听集合，返回有事件的socket，监听到了之后通知.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>events = epoll.wait()
for fd in events:
    handle(fd)
</code></pre></div></div> <p>而 ZMQ 内部维护了一个专用的 IO 线程。</p> <p>零阻塞：当你的业务代码调用 send() 时，数据其实是先进入了一个内存缓冲区。IO 线程会在后台处理真正的网络传输。</p> <p>无锁并发：ZMQ 的核心设计遵循“不要通过共享内存来通信，而要通过通信来共享内存”，极大地减少了多线程编程中的锁冲突。你只和“本地队列”打交道，真正的网络 IO 由后台 IO 线程统一处理。每个 Context 内部会启动 1 个或多个 IO 线程，这些线程统一管理所有网络 socket，负责连接、重连、读写、发送缓冲。</p> <h4 id="4-跨平台跨语言">4. 跨平台、跨语言</h4> <p>ZMQ 提供 Python、C、C++、Go 等语言接口。 进程间、主机间通信接口一致，移植成本极低。</p> <h2 id="三灵魂zmq中的消息模式">三、灵魂：ZMQ中的消息模式</h2> <h3 id="1-消息">1. 消息</h3> <p><a href="https://zeromq.cn/">ZeroMQ</a> 消息是在应用程序之间或同一应用程序组件之间传递的离散数据单元。从 ZeroMQ 本身的角度来看，消息被视为不透明的二进制数据。</p> <p>在线路上传输时，ZeroMQ 消息是大小从零开始、适合内存的任意二进制块。你可以使用 protocol buffers、msgpack、JSON 或应用程序需要通信的任何其他方式进行自己的序列化。选择一种可移植的数据表示形式是明智的，但你可以自行决定权衡。</p> <p>最简单的 ZeroMQ 消息由一个帧（也称为消息部分）组成。帧是 ZeroMQ 消息的基本线路格式。帧是一个长度指定的数据块。长度可以从零开始。ZeroMQ 保证要么传递消息的所有部分（一个或多个），要么都不传递。这允许你将帧列表作为单个在线路上传输的消息发送或接收。</p> <p>消息（单部分或多部分）必须适合内存。如果你想发送任意大小的文件，应该将其分成多块，并将每块作为单独的单部分消息发送。使用多部分数据不会减少内存消耗</p> <h3 id="2-消息模式介绍">2. 消息模式介绍</h3> <p>ZeroMQ 的 Socket API 仅仅是冰山一角，藏在朴实接口之下的，是一个丰富多彩的<strong>消息模式（Messaging Patterns）</strong>世界<label for="One" class="margin-toggle sidenote-number"></label><input type="checkbox" id="One" class="margin-toggle"/><span class="sidenote">参考<a href="https://zeromq.cn/socket-api/">ZMQ中文文档</a> 这里面写的比较详细，易懂 </span>。</p> <p>在 ZMQ 中，Socket 不是孤立存在的，它们必须成对出现（Socket Pairs）。不同的 Socket 类型组合在一起，构成了特定的拓扑结构，决定了消息是如何路由、排队以及在异常状况下如何处理的。</p> <h4 id="1-请求-应答模式-request-reply">1. 请求-应答模式 (Request-Reply)</h4> <p><strong>场景</strong>：远程过程调用 (RPC)、任务分发。</p> <p><strong>定义</strong>：<a href="https://rfc.zeromq.org/spec/28/">RFC 28/REQREP</a></p> <p>这是最常见的服务导向架构模式。它有两种主要形式：<strong>同步</strong>（严谨的乒乓交互）和<strong>异步</strong>（灵活的流量控制）。</p> <h5 id="a-同步双雄req-与-rep">A. 同步双雄：REQ 与 REP</h5> <p>这是最简单的形式，类似于“打电话”。</p> <ul> <li> <p><strong>REQ (客户端)</strong></p> <ul> <li><strong>行为</strong>：严格遵守“发送 -&gt; 接收 -&gt; 发送”的顺序。</li> <li><strong>路由</strong>： <ul> <li><strong>出站</strong>：轮询 (Round-Robin)。如果连接了多个服务，请求会轮流发给它们。</li> <li><strong>入站</strong>：匹配最后发出的请求。</li> </ul> </li> <li><strong>阻塞</strong>：如果没有可用服务，发送操作会<strong>阻塞</strong>，直到至少有一个服务上线。它非常执着，<strong>不会丢弃消息</strong>。</li> </ul> </li> <li> <p><strong>REP (服务端)</strong></p> <ul> <li><strong>行为</strong>：严格遵守“接收 -&gt; 发送 -&gt; 接收”的顺序。</li> <li><strong>路由</strong>： <ul> <li><strong>入站</strong>：公平队列 (Fair-Queuing)。从所有连接的客户端中公平地接收请求。</li> <li><strong>出站</strong>：总是路由回发送最后那个请求的客户端。</li> </ul> </li> <li><strong>异常</strong>：如果原来的请求者断线了，应答会被<strong>静默丢弃</strong>。</li> </ul> </li> </ul> <h5 id="b-异步双雄dealer-与-router">B. 异步双雄：DEALER 与 ROUTER</h5> <p>这是 ZMQ 最强大的组合，常用于构建自定义调度器、代理或高并发服务。</p> <ul> <li> <p><strong>DEALER (异步客户端/工头)</strong></p> <ul> <li><strong>定位</strong>：REQ 的异步版本。它不用傻等回复，可以像机关枪一样连续发送。</li> <li><strong>行为</strong>：<strong>全双工</strong>。发送和接收没有任何顺序限制。</li> <li><strong>路由</strong>： <ul> <li><strong>出站</strong>：轮询 (Round-Robin) 分发给后端。</li> <li><strong>入站</strong>：公平队列接收结果。</li> </ul> </li> <li><strong>静默状态</strong>：如果缓冲区满了（高水位）或没有对等方，它会阻塞发送，但<strong>绝不丢弃消息</strong>。</li> <li><strong>注意</strong>：当连接到 REP 时，DEALER 必须在消息头部手动加一个空帧作为分隔符。</li> </ul> </li> <li> <p><strong>ROUTER (异步服务端/调度中心)</strong></p> <ul> <li><strong>定位</strong>：REP 的异步版本。它是网络拓扑中的“大脑”，具有<strong>显式寻址</strong>能力。</li> <li><strong>身份魔法 (Identity)</strong>： <ul> <li><strong>接收时</strong>：ZMQ 会自动在消息最前面插入一帧，包含发送者的 <strong>路由 ID (Routing ID)</strong>。应用层通过读取这一帧知道消息是谁发的。</li> <li><strong>发送时</strong>：应用层必须把目标的 <strong>路由 ID</strong> 放在消息第一帧。ZMQ 读到这一帧，就把消息路由给对应的连接，并剥离该帧。</li> </ul> </li> <li><strong>静默状态</strong>：最无情的一个 Socket。如果目标 ID 不存在，或者目标缓冲区已满，消息会被<strong>直接丢弃 (Drop)</strong>。它不会阻塞调用者。</li> </ul> </li> </ul> <h5 id="c-混合使用">C. 混合使用</h5> <p>同步和异步可以混合使用，比如客户端可以是<code class="language-plaintext highlighter-rouge">REQ</code> 而 服务端是<code class="language-plaintext highlighter-rouge">ROUTER</code>， 意思是客户端是傻瓜式的、同步的；而服务端是精细的、异步的，这种组合允许你保留客户端代码的简单性（写个循环 send/recv 即可），同时在服务端获得极高的并发处理能力。</p> <p>想象一个银行柜台的场景：</p> <ul> <li> <p>客户端 (REQ)：是普通储户。</p> <ul> <li> <p>逻辑很简单：去窗口 -&gt; 递交存折 -&gt; 等着（阻塞） -&gt; 拿回回执 -&gt; 离开。</p> </li> <li> <p>储户不需要复杂的并发能力，一次只办一件事。</p> </li> </ul> </li> <li> <p>服务端 (ROUTER)：是那个超级业务员（或者业务分发系统）。</p> <ul> <li> <p>逻辑很复杂：它同时面对成千上万个储户。</p> </li> <li> <p>它不能因为张三存钱动作慢，就让李四一直排队。它必须能同时接收所有人的单子，处理完一个就给对应的那个储户扔回去。</p> </li> </ul> </li> </ul> <hr/> <h4 id="2-发布-订阅模式-publish-subscribe">2. 发布-订阅模式 (Publish-Subscribe)</h4> <p><strong>场景</strong>：数据分发、实时行情、日志收集。</p> <p><strong>定义</strong>：<a href="https://rfc.zeromq.org/spec/29/">RFC 29/PUBSUB</a></p> <p>这是一种一对多的广播模式，类似于“收音机”。</p> <ul> <li> <p><strong>PUB (发布者)</strong></p> <ul> <li><strong>行为</strong>：只管发，不收。</li> <li><strong>路由</strong>：<strong>扇出 (Fan-out)</strong>。一份消息会复制分发给所有当前的订阅者。</li> <li><strong>静默状态</strong>：<strong>即发即弃</strong>。如果订阅者处理不过来（达到高水位），或者根本没有订阅者，消息会被<strong>丢弃</strong>。发送永远不会阻塞。</li> <li><strong>主题</strong>：必须在消息第一帧指定“主题 (Topic)”。</li> </ul> </li> <li> <p><strong>SUB (订阅者)</strong></p> <ul> <li><strong>行为</strong>：只管收，不发。</li> <li><strong>过滤机制</strong>： <ul> <li>SUB Socket 默认是<strong>聋子</strong>（不接收任何消息）。</li> <li>必须通过 <code class="language-plaintext highlighter-rouge">ZMQ_SUBSCRIBE</code> 设置订阅主题。</li> <li><strong>前缀匹配</strong>：订阅 “topic” 会收到 “topic/A”，但收不到 “topi”。</li> <li>订阅空字符串 <code class="language-plaintext highlighter-rouge">""</code> 表示接收所有。</li> </ul> </li> </ul> </li> <li> <p><strong>XPUB &amp; XSUB (代理扩展)</strong></p> <ul> <li>用于在中间构建 Proxy。它们允许“订阅信息”像数据一样在网络中传递，从而让上游的发布者知道下游到底想要什么数据。</li> </ul> </li> </ul> <hr/> <h4 id="3-流水线模式-pipeline">3. 流水线模式 (Pipeline)</h4> <p><strong>场景</strong>：并行计算、MapReduce、多阶段任务处理。</p> <p><strong>定义</strong>：<a href="rfc.zeromq.cn/spec:30">RFC 30/PIPELINE</a></p> <p>这是一个单向的推拉结构，像工厂的流水线一样。它主要关注吞吐量和负载均衡。</p> <ul> <li> <p><strong>PUSH (推/分发者)</strong></p> <ul> <li><strong>行为</strong>：只发不收。</li> <li><strong>路由</strong>：<strong>轮询 (Round-Robin)</strong>。这也是一种天然的<strong>负载均衡器</strong>。</li> <li><strong>静默状态</strong>：如果下游节点都满了，它会<strong>阻塞</strong>，直到有空位。它保证任务不丢失。</li> </ul> </li> <li> <p><strong>PULL (拉/工入)</strong></p> <ul> <li><strong>行为</strong>：只收不发。</li> <li><strong>路由</strong>：<strong>公平队列 (Fair-Queuing)</strong>。它不会让某个来源饿死。</li> </ul> </li> </ul> <hr/> <h4 id="4-独占对模式-exclusive-pair">4. 独占对模式 (Exclusive Pair)</h4> <p><strong>场景</strong>：进程内 (inproc) 线程间通信。</p> <p><strong>定义</strong>：<a href="https://rfc.zeromq.org/spec/31/">RFC 31/EXPAIR</a></p> <ul> <li><strong>PAIR</strong> <ul> <li><strong>特点</strong>：<strong>1 对 1 绑定</strong>。它不像其他 Socket 那样支持 N:M 连接。</li> <li><strong>警告</strong>：不要用于 TCP！因为它不支持自动重连。如果连接断开，它就废了。它就是为了替代传统的线程锁（Mutex）而生的。</li> </ul> </li> </ul> <hr/> <h4 id="5-草稿模式-draft-patterns">5. 草稿模式 (Draft Patterns)</h4> <p><em>注：这些模式较新，可能需要特定的编译选项支持。</em></p> <ul> <li> <p><strong>客户端-服务器 (Client-Server)</strong></p> <ul> <li><strong>目的</strong>：为了简化 ROUTER/DEALER 的复杂性。</li> <li><strong>SERVER</strong>：类似于 ROUTER，但它不使用“消息帧”来存 ID，而是返回一个 32 位的整数 <code class="language-plaintext highlighter-rouge">routing_id</code>。发送时也是通过 API 指定 ID，而不是拼接消息帧。它是线程安全的且不会丢弃消息。</li> <li><strong>CLIENT</strong>：线程安全的客户端。</li> </ul> </li> <li> <p><strong>Radio-Dish</strong></p> <ul> <li><strong>目的</strong>：更简单的发布订阅。</li> <li><strong>特点</strong>：使用 <strong>“Group” (组)</strong> 的概念代替 “Topic”。它是<strong>精确匹配</strong>，而不是前缀匹配。长度限制 255 字节。</li> </ul> </li> </ul> <hr/> <h4 id="6-核心模式全景对比表">6. 核心模式全景对比表</h4> <table> <thead> <tr> <th style="text-align: left">Socket 类型</th> <th style="text-align: left">典型搭档</th> <th style="text-align: left">模式 / 角色</th> <th style="text-align: left">发送/接收</th> <th style="text-align: left">出站路由 (Outbound)</th> <th style="text-align: left">入站路由 (Inbound)</th> <th style="text-align: left">静默/异常行为</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>REQ</strong></td> <td style="text-align: left">REP, ROUTER</td> <td style="text-align: left">请求-应答 / 客户端</td> <td style="text-align: left">严格交替</td> <td style="text-align: left">轮询 (Round-Robin)</td> <td style="text-align: left">匹配最后请求</td> <td style="text-align: left"><strong>阻塞</strong> (直到有服务)</td> </tr> <tr> <td style="text-align: left"><strong>REP</strong></td> <td style="text-align: left">REQ, DEALER</td> <td style="text-align: left">请求-应答 / 服务端</td> <td style="text-align: left">严格交替</td> <td style="text-align: left">回复给请求者</td> <td style="text-align: left">公平队列</td> <td style="text-align: left">若客户端消失则<strong>丢弃</strong></td> </tr> <tr> <td style="text-align: left"><strong>DEALER</strong></td> <td style="text-align: left">ROUTER, REP</td> <td style="text-align: left">异步 / 客户端</td> <td style="text-align: left">双向无限制</td> <td style="text-align: left">轮询 (Round-Robin)</td> <td style="text-align: left">公平队列</td> <td style="text-align: left"><strong>阻塞</strong> (不丢消息)</td> </tr> <tr> <td style="text-align: left"><strong>ROUTER</strong></td> <td style="text-align: left">DEALER, REQ</td> <td style="text-align: left">异步 / 核心路由</td> <td style="text-align: left">双向无限制</td> <td style="text-align: left"><strong>显式寻址</strong> (根据ID)</td> <td style="text-align: left">公平队列 + 注入ID</td> <td style="text-align: left">无目标或满载时<strong>丢弃</strong></td> </tr> <tr> <td style="text-align: left"><strong>PUB</strong></td> <td style="text-align: left">SUB, XSUB</td> <td style="text-align: left">发布-订阅 / 发送方</td> <td style="text-align: left">仅发送</td> <td style="text-align: left"><strong>扇出 (Fan-out)</strong></td> <td style="text-align: left">N/A</td> <td style="text-align: left">满载或无订阅则<strong>丢弃</strong></td> </tr> <tr> <td style="text-align: left"><strong>SUB</strong></td> <td style="text-align: left">PUB, XPUB</td> <td style="text-align: left">发布-订阅 / 接收方</td> <td style="text-align: left">仅接收</td> <td style="text-align: left">N/A</td> <td style="text-align: left">公平队列</td> <td style="text-align: left"><strong>过滤</strong> (默认全丢)</td> </tr> <tr> <td style="text-align: left"><strong>PUSH</strong></td> <td style="text-align: left">PULL</td> <td style="text-align: left">流水线 / 任务源</td> <td style="text-align: left">仅发送</td> <td style="text-align: left">轮询 (负载均衡)</td> <td style="text-align: left">N/A</td> <td style="text-align: left"><strong>阻塞</strong> (等待下游)</td> </tr> <tr> <td style="text-align: left"><strong>PULL</strong></td> <td style="text-align: left">PUSH</td> <td style="text-align: left">流水线 / 任务工</td> <td style="text-align: left">仅接收</td> <td style="text-align: left">N/A</td> <td style="text-align: left">公平队列</td> <td style="text-align: left"><strong>阻塞</strong> (等待数据)</td> </tr> <tr> <td style="text-align: left"><strong>PAIR</strong></td> <td style="text-align: left">PAIR</td> <td style="text-align: left">独占 / 线程通信</td> <td style="text-align: left">双向</td> <td style="text-align: left">1对1 独占</td> <td style="text-align: left">1对1 独占</td> <td style="text-align: left"><strong>阻塞</strong> (不丢消息)</td> </tr> </tbody> </table> <h2 id="四实战用pyzmq构建一个异步客户端服务端消息流程">四、实战：用pyzmq构建一个异步客户端服务端消息流程</h2> <p>因为是异步的消息流程，因此我们将使用 DEALER 作为客户端，ROUTER 作为服务端，客户端可以连续“投递”任务，服务端可以并行处理，并根据任务完成的先后顺序异步“回邮”。</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/pyzmq/router-dealer-480.webp 480w,/assets/img/posts/pyzmq/router-dealer-800.webp 800w,/assets/img/posts/pyzmq/router-dealer-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/pyzmq/router-dealer.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Fig. 2. Router-Dealer结构图. </div> <h3 id="客户端异步的任务提交者-dealer">客户端：异步的“任务提交者” (DEALER)</h3> <p>客户端使用 DEALER，它不会像 REQ 那样因为没收到回复而锁定 send() 操作。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>import zmq
import uuid
import json
import time
import threading
from aipinfer import logger
from .utils import config_socket,SCHEDULER_ROUTER_ADDR

# 全局的 Context，ZMQ Context 是线程安全的，可以在进程内共享
# 但必须确保它是在 Worker 进程启动后创建的，或者在 fork 后重建
# 为了安全，我们也把它放在 lazy load 里
import zmq
import json
import uuid

def run_client():
    context = zmq.Context()
    socket = context.socket(zmq.DEALER)

    # 设置身份，方便服务端回邮
    client_id = b"Client-A"
    socket.setsockopt(zmq.IDENTITY, client_id)
    socket.connect("ipc:///tmp/scheduler.sock")

    # 构建任务
    task_msg = {
        "task_id": str(uuid.uuid4()),
        "payload": "Calculate AI Inference",
        "priority": "high"
    }

    print(f"发送任务: {task_msg['task_id']}")

    # 【核心点】发送多帧消息：空帧 + 序列化后的数据
    # DEALER 模拟 REQ 行为时，必须加一个空帧（Delimiter）
    socket.send_multipart([b"", json.dumps(task_msg).encode()])

    # 使用 Poller 等待结果（不阻塞整个进程）
    poller = zmq.Poller()
    poller.register(socket, zmq.POLLIN)

    if poller.poll(timeout=5000):  # 等待 5 秒
        # 接收时也要注意：第一帧是空帧，第二帧才是数据
        _, reply = socket.recv_multipart()
        print(f"收到回复: {reply.decode()}")
    else:
        print("任务超时")

    socket.close()
    context.term()
</code></pre></div></div> <h3 id="服务端精明的任务调度员-router">服务端：精明的“任务调度员” (ROUTER)</h3> <p>服务端采用 ROUTER 模式，它是整个调度器的“大脑”。与简单的 REP 不同，ROUTER 能同时“记住”成千上万个客户端的身份。</p> <ul> <li>核心逻辑： 身份剥离： 当 recv_multipart() 被调用时，ZMQ 会自动在消息头部注入客户端的 identity。这就像信封上的回信地址，调度器处理完任务后，必须靠它寄回结果。</li> </ul> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def _init_zmq_sockets(self):
    self.ctx = zmq.Context()
    # gunicorn worker &lt;-&gt; scheduler
    self.router = self.ctx.socket(zmq.ROUTER)
    self.router.bind("ipc:///tmp/scheduler_router.sock")

    self.poller = zmq.Poller()
    self.poller.register(self.router, zmq.POLLIN)

def run(self):
    self._init_zmq_sockets()

    while self._is_running:
        events = dict(self.poller.poll(timeout=100))

        if self.router in events:
            identity, _, msg = self.router.recv_multipart()
            req = json.loads(msg.decode())
            # 记住identity，回复的时候只回复给identity对应的client
            # do your job
</code></pre></div></div>]]></content><author><name></name></author><category term="programming"/><category term="computer"/><category term="communication"/><summary type="html"><![CDATA[使用PyZMQ过程中的记录。]]></summary></entry><entry><title type="html">c++中的 mutable关键字</title><link href="https://damonsj.github.io/programming/2025/03/16/mutable-keyword.html" rel="alternate" type="text/html" title="c++中的 mutable关键字"/><published>2025-03-16T01:10:00+08:00</published><updated>2025-03-16T01:10:00+08:00</updated><id>https://damonsj.github.io/programming/2025/03/16/mutable-keyword</id><content type="html" xml:base="https://damonsj.github.io/programming/2025/03/16/mutable-keyword.html"><![CDATA[<p>mutable 是用来修饰一个 const 示例的部分可变的数据成员的。如果要说得更清晰一点，就是说 mutable 的出现，将 C++ 中的 const 的概念<label for="One" class="margin-toggle sidenote-number"></label><input type="checkbox" id="One" class="margin-toggle"/><span class="sidenote">See <a href="https://en.cppreference.com/w/cpp/language/cv">cppreference cv</a> </span> 分成了两种：</p> <ul> <li> <p>二进制层面的 const，也就是「绝对的」常量，在任何情况下都不可修改(除非用 const_cast)</p> </li> <li> <p>引入 mutable 之后，C++ 可以有逻辑层面的 const，也就是对一个常量实例来说，从外部观察，它是常量而不可修改；但是内部可以有非常量的状态。当然，所谓的「逻辑 const」，在 C++ 标准中并没有这一称呼。这只是为了方便理解，而创造出来的名词。</p> </li> </ul> <p>显而易见，mutable 只能用来修饰类的数据成员；而被 mutable 修饰的数据成员，可以在 const 成员函数中修改。</p> <p>比如：</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">HashTable</span> <span class="p">{</span>
 <span class="nl">public:</span>
    <span class="c1">//...</span>
    <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">lookup</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&amp;</span> <span class="n">key</span><span class="p">)</span> <span class="k">const</span>
    <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">key</span> <span class="o">==</span> <span class="n">last_key_</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">return</span> <span class="n">last_value_</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">value</span><span class="p">{</span><span class="k">this</span><span class="o">-&gt;</span><span class="n">lookupInternal</span><span class="p">(</span><span class="n">key</span><span class="p">)};</span>

        <span class="n">last_key_</span>   <span class="o">=</span> <span class="n">key</span><span class="p">;</span>
        <span class="n">last_value_</span> <span class="o">=</span> <span class="n">value</span><span class="p">;</span>

        <span class="k">return</span> <span class="n">value</span><span class="p">;</span>
    <span class="p">}</span>

 <span class="k">private</span><span class="o">:</span>
    <span class="k">mutable</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">last_key_</span>
    <span class="k">mutable</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">last_value_</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div> <p>lookup 函数是const 的函数，正常来说是不能修改成员变量的，但是加上mutable之后，就可以修改了。</p> <p>更实际的例子是用在锁上面。比如：</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Queue</span> <span class="p">{</span>
 <span class="nl">public:</span>
    <span class="kt">void</span> <span class="n">push</span><span class="p">(</span><span class="kt">int</span> <span class="n">x</span><span class="p">);</span>
    <span class="kt">bool</span> <span class="n">empty</span><span class="p">()</span> <span class="k">const</span><span class="p">;</span>
<span class="p">};</span>

</code></pre></div></div> <p>假设这是一个线程安全的队列，empty函数是const的函数 不能修改内部成员，如果有锁，也就不能修改锁的状态，这样也就无法实现线程安全了。mutable可以实现这种情况。</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Queue</span> <span class="p">{</span>
<span class="nl">private:</span>
    <span class="k">mutable</span> <span class="n">std</span><span class="o">::</span><span class="n">mutex</span> <span class="n">mutex_</span><span class="p">;</span>
 <span class="nl">public:</span>
    <span class="kt">void</span> <span class="n">push</span><span class="p">(</span><span class="kt">int</span> <span class="n">x</span><span class="p">);</span>
    <span class="kt">bool</span> <span class="n">empty</span><span class="p">()</span> <span class="k">const</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">lock_guard</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lk</span><span class="p">(</span><span class="n">mutex_</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">};</span>

</code></pre></div></div> <p>还有一种情况使用mutable， 就是lambda函数：</p> <p>在 Lambda 表达式的设计中，捕获变量有几种方式；其中按值捕获（Caputre by Value）的方式不允许程序员在 Lambda 函数的函数体中修改捕获的变量。而以 mutable 修饰 Lambda 函数，则可以打破这种限制。</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">x</span><span class="p">{</span><span class="mi">0</span><span class="p">};</span>
<span class="k">auto</span> <span class="n">f1</span> <span class="o">=</span> <span class="p">[</span><span class="o">=</span><span class="p">]()</span> <span class="k">mutable</span> <span class="p">{</span><span class="n">x</span> <span class="o">=</span> <span class="mi">42</span><span class="p">;};</span>  <span class="c1">// okay, 创建了一个函数类型的实例</span>
<span class="k">auto</span> <span class="n">f2</span> <span class="o">=</span> <span class="p">[</span><span class="o">=</span><span class="p">]()</span>         <span class="p">{</span><span class="n">x</span> <span class="o">=</span> <span class="mi">42</span><span class="p">;};</span>  <span class="c1">// error, 不允许修改按值捕获的外部变量的值</span>
</code></pre></div></div> <p>需要注意的是，上述 f1 的函数体中，虽然我们给 x 做了赋值操作，但是这一操作仅只在函数内部生效——即，实际是给拷贝至函数内部的 x 进行赋值——而外部的 x 的值依旧是 0。</p>]]></content><author><name></name></author><category term="programming"/><category term="c++"/><summary type="html"><![CDATA[简单解释一下c++中的 mutable关键字。]]></summary></entry><entry><title type="html">c++中的 move 和 forward</title><link href="https://damonsj.github.io/programming/2025/03/09/move-forward.html" rel="alternate" type="text/html" title="c++中的 move 和 forward"/><published>2025-03-09T01:10:00+08:00</published><updated>2025-03-09T01:10:00+08:00</updated><id>https://damonsj.github.io/programming/2025/03/09/move-forward</id><content type="html" xml:base="https://damonsj.github.io/programming/2025/03/09/move-forward.html"><![CDATA[<h2 id="一左值和右值的区别">一、左值和右值的区别</h2> <p>直观意义上理解，左值是可以被赋值的，在内存中有独立的地址的值。左值在表达式结束后依然存在的、有持久身份的对象或函数。你可以对它取地址（&amp;），它通常可以出现在赋值语句的左边。比如，变量、函数返回的左值引用等。</p> <p>右值是一个表达式结束后就不复存在的临时值。你不能对它取地址（临时对象除外，但通常无意义），它通常只能出现在赋值语句的右边。比如，字面量（10, true）、函数返回的非引用类型值、a + b 的结果等。</p> <p>在c++11之前，我们只有左值引用（&amp;），它只能绑定到左值。尝试将一个左值引用绑定到右值（如临时对象）会导致编译错误（除非是 const 左值引用）。</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span><span class="o">&amp;</span> <span class="n">ref</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span> <span class="c1">// 编译错误！不能将非const左值引用绑定到右值</span>
<span class="k">const</span> <span class="kt">int</span><span class="o">&amp;</span> <span class="n">const_ref</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span> <span class="c1">// 正确。const左值引用可以延长临时对象的生命周期</span>
</code></pre></div></div> <p>c++11之后引入了右值引用(&amp;&amp;),它的核心目的就是识别出右值，并能够绑定到右值，从而允许我们对临时对象进行特殊操作。例如：</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">s1</span> <span class="o">=</span> <span class="s">"hello"</span><span class="p">;</span>
<span class="c1">// std::string&amp; l_ref = s1 + " world"; // 编译错误：s1 + " world" 是右值</span>
<span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&amp;&amp;</span> <span class="n">r_ref</span> <span class="o">=</span> <span class="n">s1</span> <span class="o">+</span> <span class="s">" world"</span><span class="p">;</span> <span class="c1">// 正确：右值引用绑定到临时对象</span>
<span class="n">r_ref</span> <span class="o">+=</span> <span class="s">"!"</span><span class="p">;</span> <span class="c1">// 正确：我们可以修改这个临时对象</span>
<span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">r_ref</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="c1">// 输出 "hello world!"</span>
</code></pre></div></div> <p>这里要注意一个非常容易混淆的点。</p> <blockquote class="block-tip"> <h5 id="tip">TIP</h5> <p>如果一个右值引用本身有名字，那么这个引用变量本身是一个左值。</p> </blockquote> <p>例如上面例子中的r_ref本身就是一个左值，因为 r_ref 作为一个变量，它在表达式结束后依然存在，你可以多次访问它，所以它符合左值的定义。这个特性是理解 std::move 和完美转发的基础。</p> <p>具体区别参考：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.cnblogs.com/gqtcgq/p/9828247.html

https://www.internalpointers.com/post/understanding-meaning-lvalues-and-rvalues-c

https://cppreference.com/w/cpp/language/value_category.html
</code></pre></div></div> <h2 id="二-stdmove">二、 std::move</h2> <p>移动语义要解决的问题是深拷贝带来的性能损耗，也就是说std::move是为了避免不必要的深拷贝而设计的。 例如：假设存在一个字符串的类: MyString，在 vector中push_back(MyString(“world”)) 这个场景中，我们创建了一个临时 MyString 对象，然后 vector 为了将它存入内部，不得不执行一次深拷贝。这个临时对象在 push_back 执行完毕后立刻被销毁。这里的拷贝是巨大的浪费：我们分配了新内存，逐字节复制了数据，而那个源数据（来自临时对象）马上就被丢弃了。如果字符串非常长，这个开销会非常大。</p> <p>移动语义的核心思想是：对于一个即将被销毁的对象（右值），我们不拷贝它的资源，而是“窃取”或“移动”它的资源。</p> <p>函数原型定义：</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="nc">T</span><span class="p">&gt;</span> <span class="c1">// in namespace std</span>
<span class="k">typename</span> <span class="n">remove_reference</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">type</span><span class="o">&amp;&amp;</span>
<span class="n">move</span><span class="p">(</span><span class="n">T</span><span class="o">&amp;&amp;</span> <span class="n">param</span><span class="p">)</span>
<span class="p">{</span>
<span class="k">using</span> <span class="n">ReturnType</span> <span class="o">=</span> <span class="c1">// alias declaration;</span>
<span class="k">typename</span> <span class="n">remove_reference</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">type</span><span class="o">&amp;&amp;</span><span class="p">;</span> <span class="c1">// see Item 9</span>
<span class="k">return</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">ReturnType</span><span class="o">&gt;</span><span class="p">(</span><span class="n">param</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>所以看出std::move只是对参数类型做了一个强制转换，转换为右值引用类型，std::move并不能移动任何东西，它唯一的功能是将一个左值或者右值强制转化为右值引用，继而可以通过右值引用使用该值，以用于移动语义。</p> <p>原型中的函数参数 T&amp;&amp; 是一个指向模板类型参数的右值引用，通过引用折叠，此参数可以与任何类型的实参匹配（可以传递左值或右值，这是std::move主要使用的两种场景）。关于引用折叠如下：</p> <p>方式一、</p> <p>X&amp; &amp;、X&amp;&amp; &amp;、X&amp; &amp;&amp;都折叠成X&amp;，用于处理左值</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">string</span> <span class="nf">s</span><span class="p">(</span><span class="s">"hello"</span><span class="p">);</span>
<span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">s</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">string</span><span class="o">&amp;</span> <span class="o">&amp;&amp;</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="err">折叠后</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">string</span><span class="o">&amp;</span> <span class="p">)</span>
<span class="err">此时：</span><span class="n">T</span><span class="err">的类型为</span><span class="n">string</span><span class="o">&amp;</span>
<span class="k">typename</span> <span class="n">remove_reference</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">type</span><span class="err">为</span><span class="n">string</span>
<span class="err">整个</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="err">被实例化如下</span>
<span class="n">string</span><span class="o">&amp;&amp;</span> <span class="n">move</span><span class="p">(</span><span class="n">string</span><span class="o">&amp;</span> <span class="n">t</span><span class="p">)</span> <span class="c1">//t为左值，移动后不能在使用t</span>
<span class="p">{</span>
<span class="c1">//通过static_cast将string&amp;强制转换为string&amp;&amp;</span>
<span class="k">return</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">string</span><span class="o">&amp;&amp;&gt;</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>方式二、</p> <p>X&amp;&amp; &amp;&amp;折叠成X&amp;&amp;，用于处理右值</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">string</span><span class="p">(</span><span class="s">"hello"</span><span class="p">))</span> <span class="o">=&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">string</span><span class="o">&amp;&amp;</span><span class="p">)</span>
<span class="c1">//此时：T的类型为string</span>
<span class="c1">//  remove_reference&lt;T&gt;::type为string</span>
<span class="c1">//整个std::move被实例如下</span>
<span class="n">string</span><span class="o">&amp;&amp;</span> <span class="n">move</span><span class="p">(</span><span class="n">string</span><span class="o">&amp;&amp;</span> <span class="n">t</span><span class="p">)</span> <span class="c1">//t为右值</span>
<span class="p">{</span>
   <span class="k">return</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">string</span><span class="o">&amp;&amp;&gt;</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>  <span class="c1">//返回一个右值引用</span>
<span class="p">}</span>
</code></pre></div></div> <p>简单来说，右值经过T&amp;&amp;传递类型保持不变还是右值，而左值经过T&amp;&amp;变为普通的左值引用.</p> <p>例如：</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">MyString</span> <span class="nf">s1</span><span class="p">(</span><span class="s">"hello"</span><span class="p">);</span>
<span class="c1">// MyString s2 = s1; // 调用拷贝构造，因为 s1 是左值</span>

<span class="n">MyString</span> <span class="n">s3</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">s1</span><span class="p">);</span> <span class="c1">// 1. std::move(s1) 将左值 s1 转换为右值引用</span>
                           <span class="c1">// 2. MyString 的构造函数重载解析选择了移动构造函数</span>
                           <span class="c1">// 3. s1 的资源被“移动”到 s3</span>

<span class="c1">// 警告！此时 s1 的内部指针已为 nullptr。</span>
<span class="c1">// s1 处于一个“有效但未指定”的状态。</span>
<span class="c1">// 对它的任何操作（除了析构或重新赋值）都可能是危险的。</span>
</code></pre></div></div> <p>参考</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> https://blog.csdn.net/p942005405/article/details/84644069

《Effective.Modern.C++-42.Specific.Ways.to.Improve.Your.Use.of.C++11.and.C++14-2014》 Item23
</code></pre></div></div> <h2 id="三-stdforward">三、 std::forward</h2> <p>完美转发是右值引用在泛型编程中的顶级应用。 例如下面的包装函数:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">target</span><span class="p">(</span><span class="n">MyString</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"target(lvalue ref)"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>
<span class="kt">void</span> <span class="nf">target</span><span class="p">(</span><span class="n">MyString</span><span class="o">&amp;&amp;</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"target(rvalue ref)"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>

<span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="nc">T</span><span class="p">&gt;</span>
<span class="kt">void</span> <span class="nf">wrapper</span><span class="p">(</span><span class="n">T</span> <span class="n">param</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// 问题：这里的 param 是左值还是右值？</span>
    <span class="c1">// 答：param 是一个有名字的变量，它永远是左值！</span>
    <span class="n">target</span><span class="p">(</span><span class="n">param</span><span class="p">);</span> <span class="c1">// 所以这里永远调用 target(lvalue ref)</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">MyString</span> <span class="n">s</span><span class="p">;</span>
    <span class="n">wrapper</span><span class="p">(</span><span class="n">s</span><span class="p">);</span> <span class="c1">// 期望调用 target(lvalue ref) -&gt; 正确</span>
    <span class="n">wrapper</span><span class="p">(</span><span class="n">MyString</span><span class="p">(</span><span class="s">"temp"</span><span class="p">));</span> <span class="c1">// 期望调用 target(rvalue ref) -&gt; 错误！实际调用了 lvalue 版本</span>
<span class="p">}</span>
</code></pre></div></div> <p>问题就在于，无论 wrapper 的实参是左值还是右值，其形参 param 在 wrapper 函数体内都是一个有名字的变量，因此它是一个左值。这就导致了值类型的“退化”，我们丢失了原始参数是左值还是右值的信息。</p> <blockquote class="block-tip"> <h5 id="tip-1">TIP</h5> <p>C++11规定了一种特殊情况，称为转发引用（也叫通用引用，Universal Reference）： 当一个函数模板的参数类型是 T&amp;&amp;，其中 T 是需要被推导的模板参数时，这个参数就是转发引用。</p> </blockquote> <p>转发引用能同时接受左值和右值，并在类型 T 中进行上述的引用折叠。 解决上述例子的问题的方法如下：</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="nc">T</span><span class="p">&gt;</span>
<span class="kt">void</span> <span class="nf">wrapper</span><span class="p">(</span><span class="n">T</span><span class="o">&amp;&amp;</span> <span class="n">param</span><span class="p">)</span> <span class="p">{</span> <span class="c1">// param 是一个转发引用</span>
    <span class="c1">// std::forward&lt;T&gt; 会检查 T 的类型：</span>
    <span class="c1">// 如果 T 是左值引用类型（U&amp;），它返回一个左值引用</span>
    <span class="c1">// 如果 T 是非引用类型（U），它返回一个右值引用</span>
    <span class="n">target</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">forward</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">(</span><span class="n">param</span><span class="p">));</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">MyString</span> <span class="n">s</span><span class="p">;</span>
    <span class="n">wrapper</span><span class="p">(</span><span class="n">s</span><span class="p">);</span> <span class="c1">// T 推导为 MyString&amp;, forward返回左值引用 -&gt; 调用 target(lvalue ref)</span>
    <span class="n">wrapper</span><span class="p">(</span><span class="n">MyString</span><span class="p">(</span><span class="s">"temp"</span><span class="p">));</span> <span class="c1">// T 推导为 MyString, forward返回右值引用 -&gt; 调用 target(rvalue ref)</span>
<span class="p">}</span>
</code></pre></div></div> <p>完美转发（Perfect Forwarding）就是利用转发引用和 std::forward 实现的，它在C++标准库中被广泛应用，例如 std::make_unique, std::vector::emplace_back 等，实现了参数传递的零成本转发。</p> <p>std::forward大致原型为：</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span><span class="o">&lt;</span> <span class="k">typename</span> <span class="nc">T</span> <span class="p">&gt;</span> <span class="kr">inline</span>
<span class="n">T</span><span class="o">&amp;&amp;</span> <span class="n">forward</span><span class="p">(</span> <span class="k">typename</span> <span class="n">std</span><span class="o">::</span><span class="n">remove_reference</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">type</span><span class="o">&amp;</span> <span class="n">t</span> <span class="p">)</span> <span class="k">noexcept</span> <span class="p">{</span>
    <span class="k">return</span> <span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&amp;&amp;&gt;</span><span class="p">(</span><span class="n">t</span><span class="p">));</span>
<span class="p">}</span>

<span class="k">template</span><span class="o">&lt;</span> <span class="k">typename</span> <span class="nc">T</span> <span class="p">&gt;</span> <span class="kr">inline</span>
<span class="n">T</span><span class="o">&amp;&amp;</span> <span class="n">forward</span><span class="p">(</span> <span class="k">typename</span> <span class="n">std</span><span class="o">::</span><span class="n">remove_reference</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">type</span><span class="o">&amp;&amp;</span> <span class="n">t</span> <span class="p">)</span> <span class="k">noexcept</span> <span class="p">{</span>
    <span class="k">static_assert</span><span class="p">(</span><span class="o">!</span><span class="n">is_lvalue_reference</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">value</span><span class="p">,</span> <span class="s">"bad forward call"</span><span class="p">);</span>
    <span class="k">return</span> <span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&amp;&amp;&gt;</span><span class="p">(</span><span class="n">t</span><span class="p">));</span>
<span class="p">}</span>
</code></pre></div></div> <p>std::forward()实现完美转发，意思是当传入参数是左值时返回左值引用，传入参数是右值时返回右值引用。</p> <ul> <li>std::move执行到右值的无条件转换。就其本身而言，它没有move任何东西。</li> <li>std::move和std::forward只不过就是执行类型转换的两个函数；std::move没有move任何东西，std::forward没有转发任何东西。在运行期，它们没有做任何事情。它们没有产生需要执行的代码，一byte都没有。</li> <li>std::forward()不仅可以保持左值或者右值不变，同时还可以保持const、Lreference、Rreference、validate等属性不变； 一个输出的例子为：</li> </ul> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;utility&gt;</span><span class="cp">
</span>
<span class="k">struct</span> <span class="nc">A</span> <span class="p">{</span>
    <span class="n">A</span><span class="p">(</span><span class="kt">int</span><span class="o">&amp;&amp;</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"rvalue overload, n="</span> <span class="o">&lt;&lt;</span> <span class="n">n</span> <span class="o">&lt;&lt;</span> <span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span> <span class="p">}</span>
    <span class="n">A</span><span class="p">(</span><span class="kt">int</span><span class="o">&amp;</span> <span class="n">n</span><span class="p">)</span>  <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"lvalue overload, n="</span> <span class="o">&lt;&lt;</span> <span class="n">n</span> <span class="o">&lt;&lt;</span> <span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span> <span class="p">}</span>
<span class="p">};</span>

<span class="k">class</span> <span class="nc">B</span> <span class="p">{</span>
<span class="nl">public:</span>
    <span class="k">template</span><span class="o">&lt;</span><span class="k">class</span> <span class="nc">T1</span><span class="p">,</span> <span class="k">class</span> <span class="nc">T2</span><span class="p">,</span> <span class="k">class</span> <span class="nc">T3</span><span class="p">&gt;</span>
    <span class="n">B</span><span class="p">(</span><span class="n">T1</span><span class="o">&amp;&amp;</span> <span class="n">t1</span><span class="p">,</span> <span class="n">T2</span><span class="o">&amp;&amp;</span> <span class="n">t2</span><span class="p">,</span> <span class="n">T3</span><span class="o">&amp;&amp;</span> <span class="n">t3</span><span class="p">)</span> <span class="o">:</span>
        <span class="n">a1_</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">forward</span><span class="o">&lt;</span><span class="n">T1</span><span class="o">&gt;</span><span class="p">(</span><span class="n">t1</span><span class="p">)},</span>
        <span class="n">a2_</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">forward</span><span class="o">&lt;</span><span class="n">T2</span><span class="o">&gt;</span><span class="p">(</span><span class="n">t2</span><span class="p">)},</span>
        <span class="n">a3_</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">forward</span><span class="o">&lt;</span><span class="n">T3</span><span class="o">&gt;</span><span class="p">(</span><span class="n">t3</span><span class="p">)}</span>
    <span class="p">{</span>
    <span class="p">}</span>

<span class="k">private</span><span class="o">:</span>
    <span class="n">A</span> <span class="n">a1_</span><span class="p">,</span> <span class="n">a2_</span><span class="p">,</span> <span class="n">a3_</span><span class="p">;</span>
<span class="p">};</span>

<span class="k">template</span><span class="o">&lt;</span><span class="k">class</span> <span class="nc">T</span><span class="p">,</span> <span class="k">class</span> <span class="nc">U</span><span class="p">&gt;</span>
<span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">make_unique1</span><span class="p">(</span><span class="n">U</span><span class="o">&amp;&amp;</span> <span class="n">u</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">(</span><span class="k">new</span> <span class="n">T</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">forward</span><span class="o">&lt;</span><span class="n">U</span><span class="o">&gt;</span><span class="p">(</span><span class="n">u</span><span class="p">)));</span>
<span class="p">}</span>

<span class="k">template</span><span class="o">&lt;</span><span class="k">class</span> <span class="nc">T</span><span class="p">,</span> <span class="k">class</span><span class="o">...</span> <span class="nc">U</span><span class="p">&gt;</span>
<span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">make_unique2</span><span class="p">(</span><span class="n">U</span><span class="o">&amp;&amp;</span><span class="p">...</span> <span class="n">u</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">(</span><span class="k">new</span> <span class="n">T</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">forward</span><span class="o">&lt;</span><span class="n">U</span><span class="o">&gt;</span><span class="p">(</span><span class="n">u</span><span class="p">)...));</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
    <span class="k">auto</span> <span class="n">p1</span> <span class="o">=</span> <span class="n">make_unique1</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span><span class="p">(</span><span class="mi">2</span><span class="p">);</span> <span class="c1">// rvalue</span>
    <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
    <span class="k">auto</span> <span class="n">p2</span> <span class="o">=</span> <span class="n">make_unique1</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span><span class="p">(</span><span class="n">i</span><span class="p">);</span> <span class="c1">// lvalue</span>

    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"B</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="k">auto</span> <span class="n">t</span> <span class="o">=</span> <span class="n">make_unique2</span><span class="o">&lt;</span><span class="n">B</span><span class="o">&gt;</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="n">i</span><span class="p">,</span> <span class="mi">3</span><span class="p">);</span>
<span class="p">}</span>

<span class="n">Output</span><span class="o">:</span>

<span class="n">rvalue</span> <span class="n">overload</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">2</span>
<span class="n">lvalue</span> <span class="n">overload</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">1</span>
<span class="n">B</span>
<span class="n">rvalue</span> <span class="n">overload</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">2</span>
<span class="n">lvalue</span> <span class="n">overload</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">1</span>
<span class="n">rvalue</span> <span class="n">overload</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">3</span>

</code></pre></div></div> <p>参考</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://blog.csdn.net/guoxiaojie_415/article/details/79902278

https://www.cnblogs.com/boydfd/p/5182743.html

https://eli.thegreenplace.net/2014/perfect-forwarding-and-universal-references-in-c/
</code></pre></div></div>]]></content><author><name></name></author><category term="programming"/><category term="c++"/><summary type="html"><![CDATA[介绍c++中的move和forward的区别及各自的实现方式。]]></summary></entry></feed>